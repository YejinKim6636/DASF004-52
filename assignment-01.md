# 심화실습-1 보고서

<!-- 
👨🏻‍💻 작성요령
아래 양식에 맞춰 내용을 채워주세요.

⚠ 주의 ⚠
단, 해시테그(`#`)로 시작하는 줄은 수정하지 마세요.
제출 내용에서 해시테그로 시작하는 줄이 없어진 경우, 항목 누락으로 간주되어 감점이 있을 수 있습니다.

👀 참고자료
[1] GitHub, GitHub에서 쓰기 위한 빠른 시작, https://docs.github.com/ko/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/quickstart-for-writing-on-github
[2] GitHub, Markdown 기본 문법, https://docs.github.com/ko/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/basic-writing-and-formatting-syntax
-->

## 1. 제출 정보

| 구분 | 내용 |
| --- | --- |
| 학년학기 | 2026학년도 1학기 |
| 과목명(학수번호) | 프로그래밍기초와실습 (DASF004-52) |
| 학번 | 2026310420 |
| 이름 | 김예진 |
| 과제명 | 심화실습-1 |

## 2. 현재 상태
 처음 접해봅니다.
### 2.1 배운적 있는 언어 및 현재 수준

- `{해당없음}` - `{해당없음}`
    - `상, 중, 하` 혹은 `해당없음`
        - 상: 어떤 요구사항을 받아도 어떻게든 구현할 수 있다.
        - 중: 기본적으로 어느정도 수준의 구현은 가능하지만 해당 언어의 고급기술 활용은 어렵다.
        - 하: 가볍게 배워본 정도이다.
- 예: Python - 상
- 예: Java - 중
- 예: C - 해당없음

### 2.2 실습 환경

| 사용한AI 도구 | 개발환경 |
| --- | --- |
| Gemini | onlineGDB(C99) |


## 3. 코드 리뷰 결과

### 3.1 AI가 생성한 코드를 얼마나 이해할 수 있었는지?

- 이해도: 6점 (0점: 전혀 이해하지 못함, 10점: 완전히 이해함)
- main에서 큰 틀을 확인 후 차례대로 일정추가, 일정조회, 일정수정, 일정삭제를 동작하는 코드입니다. 일정추가로 제목, 날짜, 시간, 설명을 추가를 합니다. 일정추가가 안된 상태로 일정조회 count=0을 하면 '저장된 일정이 없다.'고 나옵니다. 만들어둔 일정 중 하나를 골라 일정수정을 하고, 1보다 작거나 만들어준 일정보다 큰 값을 넣으면 '잘못된 번호입니다.' 가 나옵니다. 일정삭제에선 0을 입력하면 다시 돌아가고 일정수정과 마찬가지로 1보다 작거나 만들어준 일정보다 큰 값을 넣으면 '잘못된 번호입니다.' 가 나옵니다.

### 3.2 최초에 입력한 프롬프트와 그 결과물에 대한 평가

#### 최초 프롬프트

```
C언어로 콘솔 기반 일정 관리 프로그램을 작성해줘. 
필수 기능으로 일정 추가(제목, 날짜, 시간, 설명), 조회, 수정, 삭제 기능이 있어야 해.
일정이 없을 때는 "저장된 일정이 없습니다."라고 출력하고, 한 개의 .c 파일로 구현해줘.
```

#### 최종 코드 및 실행 결과

> 본 문서 가장 아래 `부록` 섹션에서 `최종 결과물 코드`와 `실행 결과`에 각각 코드 및 실행 결과를 복사 & 붙여넣기로 첨부합니다.

### 3.3 최종 결과물을 얻기까지의 경험

- 추가로 알게 된 것 정리입니다.
  1 일정추가. return: 함수를 즉시 종료, 처음주가 시 list[0], /n은 엔터랑 비슷한 개념, count++은 일정을 다음 칸에 저장되도록 1증가
  2 일정조회. for은 i가 0부터 시작하여 count직전까지 반복합니다. 프로그래밍에선 배열이 0부터 시작이지만 사람들은 1부터가 익숙하여 인덱스 표시를 했습니다.
  3 일정수정. index--;은 사람은 1부터 세지만 컴퓨터는 0부터 세기 때문에 1을 빼준겁니다.
  4 일정삭제. 배열의 중간 데이터를 삭제하면 그 자리가 비게 됩니다. 뒤에 있는 데이터를 앞으로 당겼습니다.

## 4. 결론

- 처음보는 함수와 여러 구문을 AI가 코딩을 해주었는데 새로운 경험이었습니다. 코딩이라는 것을 대학교를 와서 처음 해봤습니다. while, if, else if 등 과같은 처음보는 구문이 많았습니다. 앞으로 수업을 진행하면서 차례대로 익히고 싶습니다. 바이브코딩으로 간단한 프로그래밍을 할 수 있지만, 이를 수정 및 응용하려면 다 이해를 하고 다룰 수 있어야 합니다.

## 참고문헌

- Gemini를 참고했습니다.

## 부록

### 실행 결과

- 프로그램의 주요 기능 실행 화면을 터미널에서 텍스트를 복사&붙여넣기로 작성
- 프로그램 실행에 실패했다면, 실패와 관련된 내용을 담아도 좋음


```bash
[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 1

--- 일정 추가 ---
제목: 과제
날짜(YYYY-MM-DD): 2026-03-15
시간(HH:MM): 01:45 
설명: 프기실
일정이 성공적으로 추가되었습니다!

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 1

--- 일정 추가 ---
제목: 학교가기
날짜(YYYY-MM-DD): 2026-03-17
시간(HH:MM): 11:00
설명: 등교
일정이 성공적으로 추가되었습니다!

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 2

--- 일정 목록 ---
[1] 제목: 과제 | 날짜: 2026-03-15 | 시간: 01:45
    설명: 프기실
[2] 제목: 학교가기 | 날짜: 2026-03-17 | 시간: 11:00
    설명: 등교

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 3

--- 일정 목록 ---
[1] 제목: 과제 | 날짜: 2026-03-15 | 시간: 01:45
    설명: 프기실
[2] 제목: 학교가기 | 날짜: 2026-03-17 | 시간: 11:00
    설명: 등교

수정할 일정 번호를 선택하세요: 1
새 제목: 놀기
새 날짜(YYYY-MM-DD): 2026-03-16
새 시간(HH:MM): 09:22
새 설명: 노는중
수정이 완료되었습니다!

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 2

--- 일정 목록 ---
[1] 제목: 놀기 | 날짜: 2026-03-16 | 시간: 09:22
    설명: 노는중
[2] 제목: 학교가기 | 날짜: 2026-03-17 | 시간: 11:00
    설명: 등교

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 3

--- 일정 목록 ---
[1] 제목: 놀기 | 날짜: 2026-03-16 | 시간: 09:22
    설명: 노는중
[2] 제목: 학교가기 | 날짜: 2026-03-17 | 시간: 11:00
    설명: 등교

수정할 일정 번호를 선택하세요: 2
새 제목: 작업
새 날짜(YYYY-MM-DD): 2026-03-18
새 시간(HH:MM): 19:43
새 설명: 일일작업
수정이 완료되었습니다!

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 4

--- 일정 목록 ---
[1] 제목: 놀기 | 날짜: 2026-03-16 | 시간: 09:22
    설명: 노는중
[2] 제목: 작업 | 날짜: 2026-03-18 | 시간: 19:43
    설명: 일일작업

삭제할 일정 번호를 선택하세요: 2
일정이 삭제되었습니다.

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 2

--- 일정 목록 ---
[1] 제목: 놀기 | 날짜: 2026-03-16 | 시간: 09:22
    설명: 노는중

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 4

--- 일정 목록 ---
[1] 제목: 놀기 | 날짜: 2026-03-16 | 시간: 09:22
    설명: 노는중

삭제할 일정 번호를 선택하세요: 1
일정이 삭제되었습니다.

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 2

--- 일정 목록 ---
저장된 일정이 없습니다.

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 3

--- 일정 목록 ---
저장된 일정이 없습니다.

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 4

--- 일정 목록 ---
저장된 일정이 없습니다.

[일정 관리 프로그램]
1.추가 2.조회 3.수정 4.삭제 0.종료
메뉴 선택: 0
프로그램을 종료합니다.
```

### 최종 결과물 코드

- AI가 생성한 전체 코드 첨부

```c

#include <stdio.h>
#include <string.h>

#define MAX_SCHEDULES 100

typedef struct {
    char title[50];
    char date[20];
    char time[10];
    char desc[100];
} Schedule;

Schedule list[MAX_SCHEDULES];
int count = 0;

// 일정 추가
void addSchedule() {
    if (count >= MAX_SCHEDULES) {
        printf("\n[오류] 저장 공간이 가득 찼습니다.\n");
        return;
    }
    printf("\n--- 일정 추가 ---\n");
    printf("제목: "); scanf(" %[^\n]s", list[count].title);
    printf("날짜(YYYY-MM-DD): "); scanf("%s", list[count].date);
    printf("시간(HH:MM): "); scanf("%s", list[count].time);
    printf("설명: "); scanf(" %[^\n]s", list[count].desc);
    count++;
    printf("일정이 성공적으로 추가되었습니다!\n");
}

// 일정 조회
void showSchedules() {
    printf("\n--- 일정 목록 ---\n");
    if (count == 0) {
        printf("저장된 일정이 없습니다.\n");
        return;
    }
    for (int i = 0; i < count; i++) {
        printf("[%d] 제목: %s | 날짜: %s | 시간: %s\n", i + 1, list[i].title, list[i].date, list[i].time);
        printf("    설명: %s\n", list[i].desc);
    }
}

// 일정 수정
void updateSchedule() {
    int index;
    showSchedules();
    if (count == 0) return;
    
    printf("\n수정할 일정 번호를 선택하세요: ");
    scanf("%d", &index);
    
    if (index < 1 || index > count) {
        printf("잘못된 번호입니다.\n");
        return;
    }
    
    index--; // 배열 인덱스 맞춤
    printf("새 제목: "); scanf(" %[^\n]s", list[index].title);
    printf("새 날짜(YYYY-MM-DD): "); scanf("%s", list[index].date);
    printf("새 시간(HH:MM): "); scanf("%s", list[index].time);
    printf("새 설명: "); scanf(" %[^\n]s", list[index].desc);
    printf("수정이 완료되었습니다!\n");
}

// 일정 삭제
void deleteSchedule() {
    int index;
    showSchedules();
    if (count == 0) return;
    
    printf("\n삭제할 일정 번호를 선택하세요: ");
    scanf("%d", &index);
    
    if (index < 1 || index > count) {
        printf("잘못된 번호입니다.\n");
        return;
    }
    
    for (int i = index - 1; i < count - 1; i++) {
        list[i] = list[i + 1];
    }
    count--;
    printf("일정이 삭제되었습니다.\n");
}

int main() {
    int menu;
    while (1) {
        printf("\n[일정 관리 프로그램]");
        printf("\n1.추가 2.조회 3.수정 4.삭제 0.종료\n메뉴 선택: ");
        scanf("%d", &menu);
        
        if (menu == 1) addSchedule();
        else if (menu == 2) showSchedules();
        else if (menu == 3) updateSchedule();
        else if (menu == 4) deleteSchedule();
        else if (menu == 0) {
            printf("프로그램을 종료합니다.\n");
            break;
        } else {
            printf("잘못된 입력입니다.\n");
        }
    }
    return 0;
}
